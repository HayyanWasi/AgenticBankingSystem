## For RAG 
### Run: 
##### One-time ingestion (only when adding new PDFs):
"uv run python -m app.rag.pipeline"
##### Run the agent (fast — no re-embedding):
"uv run python -m app.agents.privacy_policy_agent"

